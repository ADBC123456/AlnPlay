# AlnPlay 媒体库与弹幕修复验证报告

验证日期：2026-09-20

## 已完成

- 媒体库快照查询使用按实例索引；失效 episodeId 仅在非手动绑定且文件名明确包含 SxxEyy 时兼容匹配。
- 启动和回到前台会后台刷新过期目录；重复刷新合并，自动失败冷却 5 分钟。
- 文件发现先于 TMDB 元数据完成落库；单轮成功的搜索、详情和整季结果复用，扫描进度限频至 250ms。
- missing 文件保留历史根目录，详情页仍可手动刷新；无版本剧集区分正在更新、来源更新失败和未找到文件。
- 弹幕统一写入 `danmaku:<resumeKey>`，读取兼容旧原始键，写入时迁移去重并保护手动绑定。
- 强制刷新贯通保存绑定、自动匹配、元数据匹配和仓储；同键写入串行，迟到请求不能覆盖新缓存。
- 空结果有效期为 5 分钟；协议拒绝、非零 errorCode、全量解析失败和声明数量不一致不会缓存成合法空结果。
- 多来源绑定会在空结果或失败后继续；汇总优先级为有内容、失败、全部有效为空。
- 视频准备与弹幕加载并行，换集/重载由播放器代次校验隔离旧结果。

## 自动验证

- `flutter analyze --no-pub`：通过，0 issues。
- 媒体库、扫描、元数据、历史定位、详情页、弹幕身份、仓储、服务和批量刮削相关测试：79 项通过。
- 新增覆盖：旧身份键迁移、空缓存过期、强制刷新竞态、手动剧集绑定保护、missing 历史根目录。
- 全量 `flutter test --no-pub`：媒体库与弹幕相关失败已全部修复；剩余 2 项失败均来自 `image_cache_service_test.dart` 的 Windows 路径分隔符断言，期望 `/` 而实际路径为 `\\`，与本次媒体库/弹幕改动无关。
- `git diff --check`：通过。

## 环境限制

- Android APK 未构建：当前主机未安装或未配置 Android SDK，Flutter 报告 `No Android SDK found`。
- iOS 未签名 IPA 未构建：当前主机为 Windows，无法运行 Xcode/iOS 构建。
- 未进行 vivo 真机验收。仍需在目标设备核对 151–158 集、末段文件、刷新状态，以及第 157 集的绑定来源、远程 episodeId、原始/解析条数和缓存命中。
- 未执行 release/profile 首屏 500ms 性能测量；代码层已消除重复全量解析及视频等待弹幕的主要阻塞点。
